function Hamburger(props) {

    return (
        <button
            id='mobile-open-btn'
            className={props.className}>
            &#9776;
        </button>
    )
}

export default Hamburger;